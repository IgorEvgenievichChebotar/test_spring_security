create table Client(
    id int generated by default as identity primary key ,
    username varchar(100) unique not null ,
    password varchar(100) not null ,
    name varchar(100),
    birthdate int check ( birthdate>1900 ) check ( birthdate<2022 )
)

insert into client(username, password, name, birthdate) VALUES ('Pavlov', 'qwerty', 'Павлов Андрей Степанович', 1990)